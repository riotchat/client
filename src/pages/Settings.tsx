import React from 'react';
import styles from './Settings.module.scss';

export function Settings() {
    return (
        <div className={styles.settings}>
            design settings here
        </div>
    );
}